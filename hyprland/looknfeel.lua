-- Change the default Omarchy look'n'feel.

-- Keep the pointer in place when changing workspaces or focusing windows.
hl.config({
  cursor = {
    no_warps = true,
    warp_on_change_workspace = 0,
  },
})
